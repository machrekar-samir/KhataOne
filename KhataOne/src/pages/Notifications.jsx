import { Heading } from '../components/PageParts.jsx'
import { useApp } from '../context/AppContext.jsx'
export default function Notifications() { const { data } = useApp(); return <Heading eyebrow="ACTIVITY CENTER" title="Notifications"><div className="notification-panel panel"><div className="notification-group"><p className="eyebrow">TODAY</p>{data.notifications.length ? data.notifications.map((item) => <div className="notification" key={item.id}><span className="notification-mark">✦</span><div><strong>{item.text}</strong><p>KhataOne activity</p></div><i className={item.read ? 'read' : ''} /></div>) : <p className="subheading">You are all caught up.</p>}</div><div className="notification-group"><p className="eyebrow">EARLIER</p><div className="notification"><span className="notification-mark muted-mark">✓</span><div><strong>Your workspace is ready</strong><p>Start by adding a customer or transaction.</p></div></div></div></div></Heading> }