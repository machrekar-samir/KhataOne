import { useApp } from '../context/AppContext.jsx'; import { Heading, PanelHeading } from '../components/PageParts.jsx'; import { money } from '../utils/calculations.js'; import { paymentPrediction } from '../utils/paymentPrediction.js'
export default function AIInsights() { const { customers, totals } = useApp(); return <Heading eyebrow="RULE-BASED INTELLIGENCE" title="AI Insights"><div className="insight-grid"><div className="panel"><PanelHeading eyebrow="TRUST SCORES" title="Customer trust" />{customers.map((item) => <div className="insight-line" key={item.id}><span>{item.name}</span><strong>{item.score}/100</strong><small>{item.status}</small></div>)}</div><div className="panel"><PanelHeading eyebrow="RECOMMENDATIONS" title="What to do next" /><div className="recommendation-stack"><p>Focus on recovering <strong>{money(totals.overdue)}</strong> in overdue payments.</p><p>Average predicted payment probability: <strong>{paymentPrediction(customers)}%</strong>.</p></div></div></div></Heading> }