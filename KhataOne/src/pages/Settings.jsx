import { useState } from 'react'; import { useApp } from '../context/AppContext.jsx'; import { Heading } from '../components/PageParts.jsx'
export default function Settings() { const { data, update, notify } = useApp(); const [business, setBusiness] = useState(data.business); return <Heading eyebrow="WORKSPACE CONTROL" title="Settings"><form className="settings-form" onSubmit={(event) => { event.preventDefault(); update({ business }); notify('Business settings saved') }}><label>Business name<input value={business.name} onChange={(event) => setBusiness({ ...business, name: event.target.value })} /></label><label>Business type<input value={business.type} onChange={(event) => setBusiness({ ...business, type: event.target.value })} /></label><label>Phone<input value={business.phone} onChange={(event) => setBusiness({ ...business, phone: event.target.value })} /></label><label>Email<input value={business.email} onChange={(event) => setBusiness({ ...business, email: event.target.value })} /></label><label>Address<textarea value={business.address} onChange={(event) => setBusiness({ ...business, address: event.target.value })} /></label><button className="add-button">Save settings</button></form></Heading> }