import { useApp } from '../context/AppContext.jsx'; import { Heading } from '../components/PageParts.jsx'
export default function Reports() { const { data, customers } = useApp(); const exportCsv = () => { const rows = [['Customer', 'Type', 'Amount', 'Date'], ...data.transactions.map((item) => [customers.find((customer) => customer.id === item.customerId)?.name, item.type, item.amount, item.date])]; const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob([rows.map((row) => row.join(',')).join('\n')], { type: 'text/csv' })); link.download = 'khataone-report.csv'; link.click() }; return <Heading eyebrow="BUSINESS INTELLIGENCE" title="Reports" action="Export CSV" onAction={exportCsv}><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{['Daily collection', 'Monthly collection', 'Outstanding report', 'Overdue report', 'Customer report', 'Risk report'].map((name) => <div className="rounded-xl border border-[#ebe7e3] bg-white p-5 dark:border-[#423238] dark:bg-[#2b2226]" key={name}><p className="mb-2 text-[9px] font-bold tracking-[1.2px] text-[#aaa0a0]">REPORT</p><h2 className="font-serif text-lg font-bold">{name}</h2><strong className="mt-5 block text-sm text-[#8b8383]">{data.transactions.length} records</strong></div>)}</div></Heading> }