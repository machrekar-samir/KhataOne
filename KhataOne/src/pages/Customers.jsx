import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useApp } from '../context/AppContext.jsx'; import { Heading, CustomerRow } from '../components/PageParts.jsx'; import EmptyState from '../components/EmptyState.jsx'
export default function Customers() { const { customers, deleteCustomer } = useApp(); const [query, setQuery] = useState(''); const navigate = useNavigate(); const items = customers.filter((item) => item.name.toLowerCase().includes(query.toLowerCase())); return <Heading eyebrow="CUSTOMER DIRECTORY" title="Customers" action="Add customer" onAction={() => navigate('/transactions')}><div className="toolbar"><input placeholder="Search customers" value={query} onChange={(event) => setQuery(event.target.value)} /></div><div className="table-list">{items.map((customer) => <div className="table-row" key={customer.id}><CustomerRow customer={customer} onClick={() => navigate(`/customers/${customer.id}`)} /><div className="row-actions"><button onClick={() => navigate(`/customers/${customer.id}`)}>Edit</button><button onClick={() => deleteCustomer(customer.id)}>Delete</button></div></div>)}{!items.length && <EmptyState text="Start by adding your first customer." />}</div></Heading> }