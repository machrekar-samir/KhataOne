import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useApp } from '../context/AppContext.jsx'; import { Heading, CustomerRow } from '../components/PageParts.jsx'; import EmptyState from '../components/EmptyState.jsx'
export default function Customers() { const { customers, deleteCustomer } = useApp(); const [query, setQuery] = useState(''); const navigate = useNavigate(); const items = customers.filter((item) => item.name.toLowerCase().includes(query.toLowerCase())); return <Heading eyebrow="CUSTOMER DIRECTORY" title="Customers" action="Add customer" onAction={() => navigate('/transactions')}><div className="mb-4"><input className="w-full rounded-lg border border-[#ebe7e3] bg-white px-3 py-2.5 text-sm outline-none focus:border-[#8f2039] dark:border-[#423238] dark:bg-[#2b2226]" placeholder="Search customers" value={query} onChange={(event) => setQuery(event.target.value)} /></div><div className="overflow-hidden rounded-xl border border-[#ebe7e3] bg-white dark:border-[#423238] dark:bg-[#2b2226]">{items.map((customer) => <div className="border-b border-[#ebe7e3] last:border-0 dark:border-[#423238]" key={customer.id}><CustomerRow customer={customer} onClick={() => navigate(`/customers/${customer.id}`)} /><div className="flex gap-3 px-3 pb-3 text-xs"><button className="font-bold text-[#8f2039]" onClick={() => navigate(`/customers/${customer.id}`)}>Edit</button><button className="text-[#bf6873]" onClick={() => deleteCustomer(customer.id)}>Delete</button></div></div>)}{!items.length && <EmptyState text="Start by adding your first customer." />}</div></Heading> }