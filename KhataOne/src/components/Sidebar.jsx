import { NavLink } from 'react-router-dom'
import { useApp } from '../context/AppContext.jsx'
import { Bot, Bell, CalendarDays, ChartNoAxesCombined, Cog, LayoutDashboard, Network, Receipt, Sparkles, UsersRound, WalletCards } from 'lucide-react'
const items = [['Dashboard', '/overview', LayoutDashboard], ['Customers', '/customers', UsersRound], ['Transactions', '/transactions', Receipt], ['Collections', '/collections', WalletCards], ['AI Insights', '/ai-insights', Sparkles], ['Payment Calendar', '/payment-calendar', CalendarDays], ['Automation', '/automation', Network], ['Reports', '/reports', ChartNoAxesCombined], ['Team', '/team', UsersRound], ['Notifications', '/notifications', Bell], ['Settings', '/settings', Cog]]
export default function Sidebar() { const { data } = useApp(); return <aside className="sidebar"><div className="reference-brand"><span className="reference-brand-mark">K1</span><div><strong>KhataOne</strong><small>Recover it smarter</small></div></div><nav className="main-nav">{items.map(([label, path, Icon]) => <NavItem key={path} label={label} path={path} Icon={Icon} />)}</nav><div className="sidebar-bottom"><div className="reference-ai-card"><Bot size={17} /><strong>KhataOne AI</strong><p>Ask anything about your pending money.</p><NavLink to="/ai-insights">Open assistant <span>→</span></NavLink></div><div className="reference-sidebar-user"><div className="profile-avatar">{data.profile.name.slice(0, 2).toUpperCase()}</div><div><strong>{data.profile.name}</strong><small>Owner account</small></div></div></div></aside> }
function NavItem({ label, path, Icon }) { return <NavLink className={({ isActive }) => isActive ? 'nav-item active' : 'nav-item'} to={path}><Icon className="nav-icon" size={15} strokeWidth={1.8} />{label}</NavLink> }