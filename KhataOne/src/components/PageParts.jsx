import { money } from '../utils/calculations.js'
export function Heading({ eyebrow, title, action, onAction, children }) { return <><section className="welcome-row"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1></div>{action && <button className="add-button" onClick={onAction}>＋ {action}</button>}</section>{children}</> }
export function Stat({ label, value, icon, tone = '', featured = false }) { return <div className={`stat-card ${featured ? 'featured' : ''}`}><div className="stat-head"><span>{label}</span><span className={`stat-icon ${tone}-icon`}>{icon}</span></div><strong>{value}</strong><div className="stat-foot"><span className="positive">Live</span><span>calculated now</span></div></div> }
export function CustomerRow({ customer, onClick }) { return <button className="customer-row" onClick={onClick}><div className={`customer-avatar ${customer.color}`}>{customer.name.slice(0, 2).toUpperCase()}</div><div className="customer-info"><strong>{customer.name}</strong><span><i className={`status-dot ${customer.color}`} />{customer.status}</span></div><div className="customer-score"><small>Trust score</small><strong>{customer.score}<small>/100</small></strong></div><div className="customer-amount"><small>Outstanding</small><strong>{money(customer.outstanding)}</strong></div><span className="row-arrow">↗</span></button> }
export function PanelHeading({ eyebrow, title, children }) { return <div className="panel-heading"><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2></div>{children}</div> }